# -*- coding: utf-8 -*-
"""04-1. 통계로 요약하기_202010678표성민_실습제출용

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1f0BwtwClSduntXP7RSWxrJPqAey7X01k

** 04-1. 통계로 요약하기 **
전체 데이터를 몇 개의 숫자로 요약할 수 있다면, 현재 데이터의 특징을 이해하고 다른 데이터와 차이점을 쉽게 설명할 수 있습니다.

**기술통계 구하기**
기술통계 = 자료의 내용을 압축하여 설명하는 방법, 요약통계(Summary statistics) 라고도 부름
정량적인 수치로 전체 데이터 특징을 요약하거나 이해하기 쉬운 간단한 그래프를 사용합니다. 대표적인 통계량에는 평균, 표준편차 등이 있습니다.
데이터 시각화를 아우르는 이러한 데이터 분석 방법 = 탐색적 데이터 분석(exploratory data analysis)

Let's start
"""

import gdown

gdown.download('https://bit.ly/3736JW1', 'ns_book6.csv', quiet=False)

import pandas as pd

ns_book6 = pd.read_csv('ns_book6.csv', low_memory=False)
ns_book6.head()

"""판다스는 데이터프레임에서 기본적인 기술통게를 자동으로 추출해주는 메서드가 있습니다.
describe()메서드
"""

ns_book6.describe()

"""count: 누락된 값을 제외한 데이터 개수
mean : 평균
std: 표준편차
min: 최솟값
50%: 중앙값
25%와 75% 순서대로 늘어 놓았을 때, 25% 지점과 75%지점에 놓인 값
max: 최댓값

여기서 도서권수 최솟값이 0이면 걸러야겠쥬?
3장_ 불리언배열과 sum()함수를 활용해 도서권수가 0인 도서의 행 개수를 쉽게 카운트할 수 있습니다.
"""

(ns_book6['도서권수']==0).sum()

"""3206개의 도서권수가 0입니다. 전체 데이터에서 1%도 채 안 되기 때문에 무시합시다.
만약 이런 도서과 꽤 많다면, 어떤이유에서 0권인지, 제외할지 판단해야합니다.
처리 기준은 해결할 문제에 따라 다르며, 이러한 결정이 분석 결과에 큰영향을 미칠 수도 있습니다.
이 책에서는 실제로 없는 도서 대출 데이터는 의미가 없다고 판단하여 제외하겠습니다.
"""

ns_book7 = ns_book6[ns_book6['도서권수']>0]

"""다시 기술통계를 출력해봅시다.
describe()메서드는 기본적으로 25%, 50%, 75%에 위치한 값을 보여줍니다.
예로 들어, 30%, 60%, 90%에 위치한 값을 보고 싶다면 소수점으로 0.3, 0.6, 0.9를 지정합니다.
"""

ns_book7.describe(percentiles=[0.3, 0.6, 0.9])

"""열의 데이터 타입이 수치가 아닌 다른 데이터 타입의 열의 기술통계를 보고 싶다면, include매개변수의 데이터 타입을 지정할 수 있습니다."""

ns_book7.describe(include='object')

"""count행=누락된 값을 제외한 데이터 개수를 나타내고, unique행은 고유한 값의 개수를 나타냅니다.
top행: 가장 많이 등장하는 값
freq행: top행에 등장하는 항목의 빈도수
싱행결과에 따르면, '도서명'열에서 가장 많이 등장하는 데이터는 '승정원일기'로 250회나 나오네요.
이제 평균, 중앙값 등 개발 통계량에 조금 더 살펴봅시다.

**평균**
"""

x = [10, 20, 30]
sum = 0
for i in range(3):
  sum += x[i]
print("평균:", sum/len(x))

"""판다스 데이터프레임과 시리즈 객체는 평균을 나타내는 mean()메서드를 제공합니다.
평균대출 건수는 약 11.6으로 describe()메서드를 출력해서 본 것과 같습니다.
"""

ns_book7['대출건수'].mean()

"""**중앙값(meadian)**

중앙값은 전체 데이터를 순서대로 늘어 놓았을 때 중앙에 위치한 값

앞서 describe()메서드의 기본 출력에서 25%, 50%, 75%위치의 값이 출력됩니다.

이중에 50%위치의 값이 중앙값에 해당됩니다.

판다스는 중앙값을 구하기 위한 메서드를 따로 제공합니다.

중앙값이 그만큼 자주 사용되고 중요하다는 의미겠죠!!

1 2 5(중앙값) 10 20
"""

ns_book7['대출건수'].median()

"""데이터 개수가 홀수면, 중앙값을 결정하기 쉽습니다.
하지만 짝수일 때는 어떻게 중앙값을 결정할까요?
: 가운데 두 개의 값을 평균하여 중앙값을 결정합니다.
"""

temp_df = pd.DataFrame([1,2,3,4])
temp_df.median()

"""**중복값 제거하고 중앙값 구하기**

같은 값의 대출건수를 모두 제외하고 중앙값을 계산할 수 있습니다.

drop_duplicate()메서드를 사용해'대출건수'열에서 중복된 값을 가진 행을 제거한 후, 중앙값을 구해보겠습니다.
"""

ns_book7['대출건수'].drop_duplicates().median()

"""중복된 값을 제거하니 중앙값이 엄청나게 높아졌습니다.

짐작하건대, 작은 대출건수에 중복된 행이 많다고 생각할 수 있습니다.

얼마나 많은 값이 중복되었을 지는 추후 그래프를 그려보면서 알아봅시다.

**최솟값, 최댓값**

중앙값과 함께 자주 사용되는 통계량은 **최솟값과 최댓값**입니다.

판다스에서는 각각**min()메서드와 max()메서드**를 제공!!
"""

ns_book7['대출건수'].min()

ns_book7['대출건수'].max()

"""**분위수(quartile)**
분위수: 데이터를 순서대로 늘어 놓았을 때 이를 균등한 간격으로 나누는 기준점입니다.
예를 들어 이분위수는 전체 데이터를 두 구간으로 나누기 때문에 중앙값에 해당합니다.
많이 사용하는 사분위수는 순서대로 정렬된 데이터를 4구간을 나눕니다.
따라서 사분위수는 3개가 나오고 25%, 50%, 75%에 해당합니다.
25%에 대항하는 값을 제1 사분위수, 중앙값을 제2사분위수, 75%에 해당하는 값을 제3사분위수 라고도 부릅니다.

##quantile()메서드
판다스에서 분위수 값을 계산할 때는 quantile()메서드를 사용합니다.
하위 25%에 위치한 값을 출력하려면 0.25를 입력합니다.
"""

ns_book7['대출건수'].quantile(0.25)

"""quantile()메서드에 여러 개의 분위수를 지정할 수도 있습니다.
다음처럼 25%, 50%, 75%에 위치한 값을 추출해보겠습니다.
"""

ns_book7['대출건수'].quantile([0.25, 0.5, 0.75])

"""이처럼 qunatile()메서드에 여러 개의 분위수를 지정하면, 각 분위수에 해당하는 값을 담은 시리즈 객체를 반환합니다.

그렇다면 다음과 같이 1,2,3,4,5 다섯 개의 숫자가 있을 때, 90% 위치에 있는 값은 얼마일까요?
시리즈 객체를 정의한 후 분위수를 구합니다.
"""

pd.Series([1,2,3,4,5]).quantile(0.9)

"""시리즈 객체에 4.6이라는 값이 없는데, 출력되었네요. 이 값은 어떻게 계산되었을까요?

quantile()메서드는 **interpolation매개변수**에서 중간 값을 계산하는 방법을 결정합니다.
위 코드는 interpolation매개변수에서 중간 값을 계산하는 방법을 결정합니다.
위 코드는 interpolation매개변수를 따로 지정하지 않아서 **기본값**으로 계산되었습니다.

예를 들어 4와 5 사이에서 90%에 해당하는 분위수를 찾을 때, 다음 그림처럼 4의 분위수 0.75와 5의 분위수 1.0을 사용해 간단한 비례식을 세울 수 있습니다.

여기서 궁금한 값b에 대해 정리하면 아래와 같은식을 얻게 됩니다.

책 참고하시오.

이렇게 두 지점 사이에 놓인 특정 위치의 값을 구하는 방법을 보간(interploation)이라고 부릅니다.
"""

4+(0.9-0.75)*(5-4)/(1.0-0.75)

pd.Series([1,2,3,4,5]).quantile(0.9, interpolation='midpoint')

"""interpolation매개변수는 'linear'외에도 여러 가지 다른 보간 방식을 제공.
'midpoint'는 분위수에 상관없이 무조건 두 수 사이의 중앙값을 사용합니다.

"""

pd.Series([1,2,3,4,5]).quantile(0.9, interpolation='nearest')

"""'nearest'는 두 수 중에서 가까운 값을 선택합니다. 이 경우는 0.9가 1.0에 더 가깝기 때문에 5가 됩니다.

**백분위구하기**
앞에서는 분위수를 직접 지정해서 구했습니다.
이번에는 반대로 해보겠습니다.

1,2,3,4,5에서 4의 백분위는 0.75라고 쉽게 찾을 수 있습니다.

데이터가 작을 때는 이렇게 직접 쉽게 헤아려 구할 수 있지만 데이터가 많으면 어렵습니다.

예를들어 남산 도서관 대출 데이터에서 대출건수 10이 위치한 백분위를 어떻게 찾을 수 있을까요?

판다스에서 이에 대한 메서드를 제공하지는 않지만, 간단한 트릭으로 유사한 결과를 얻을 수 있습니다.

먼저 '대출건수'열의 값이 10보다 작은지 비교하여 불리언배열을 만듭니다.
"""

borrow_10_flag = ns_book7['대출건수'] < 10

"""이렇게 변환한 borrow_10_flag배열(실제로는 시리즈 객체)에서 True인 개수를 모두 헤아린 후 전체 데이터 개수로 나누면 10보다 작은 대출 건수의 비율을 얻을 수 있습니다.

데이터의 양이 많다면 전체 데이터를 순서대로 놓았을 때 10이 위치한 백분위와 거의 비슷하게 됩니다.

판다스에서 불리언 자료를 산술 연산하면 True는 1, False는 0으로 취급합니다.

따라서, 다음처럼 borrow_10_flag배열에서 mean()메서드를 호출하여 평균을 구하면 10보다 작은 값이 차지하는 비율을 간단히 얻을 수 있습니다.

(1의 개수 합) ÷ (전체 데이터 개수)

1은 곧 True(대출건수 10 미만)이므로, 결과적으로 "전체 중 10보다 작은 데이터가 차지하는 비율"이 딱 나옵니다.

이 방식은 정렬할 필요 없이 "10보다 작니?"라고 물어보고 1의 개수만 세면 되기 때문에 데이터 양이 많을 때 훨씬 빠르고 간편합니다.
"""

borrow_10_flag.mean()

"""그럼 10에 대한 백분위는 0.65정도가 되겠군요.
 quantile()메서드에 백분위를 넣어 직접확인해보면 기대했던 10이 출력됩니다.
"""

# 백분위 0.65에서 10이 나오는지 역산
ns_book7['대출건수'].quantile(0.65)

"""
**분산**
분산은 평균으로부터 데이터가 얼마나 퍼져있는지를 나타내는 통계량입니다.
데이터가 가운데 모여있다면 분산이 작음.
넓게 퍼져있다면 분산이 큼.

분산은 데이터의 각 값에서 평균을 뺀 다음 제곱한 후 평균처럼 샘플 개수로 나누어 구할 수 있습니다."""

ns_book7['대출건수'].var()

"""분산=데이터가 평균에서 얼마나 퍼져있는지 나타내는 값
이 결과만 보고 값이 작은지 큰지 판단하기 어렵습니다.
여기에서는 분산을 이렇게 구할 수 있다 정도로 기억하고 넘어가겠습니다.

분산은 제곱을 했기 때문에 데이터가 평균에서 멀어질수록 값이 급격히 커집니다.
또 단위가 대출건수의 제곱이 되어 해석하기 조금 어렵습니다.
이를 보완하기 위해 분산에 제곱근을 취한 **표준편차**를 사용합니다.

**표준편차(Standard deviation)구하기**
표준편차는 분산에 제곱근을 한 것으로 수식기호는 s를 사용합니다.
반대로, 분산 = 표준편차^2
분산을 s제곱으로 표시
"""

ns_book7['대출건수'].std()

"""이 값을 어떻게 해석할까?
앞서 도서대출 건수는 평균 11회가 조금 넘었음.
어떤 도서의 대출 건수를 확인했을 때 평균보다 19만큼 더 많거나 적을 수 있다는 의미입니다.
평균이 11정도인데, 표준편차가 19이면 너무 크지 않나요?
게다가 대출건수는 음수가 될 수 없는데 말이죠, 이상하죠?
아마도 평균보다 훨씬 큰 대출건수가 있기 때문에 표준편차가 큰 것으로 예상할 수 있습니다.

**수식을 코드로 구현해보자**

판다스와 넘파이를 사용해 표준편차를 수식대로 직접계산해서 판다스의 std()메서드를 사용한 결과와 동일한지 확인해보자.
"""

import numpy as np

diff = ns_book7['대출건수'] - ns_book7['대출건수'].mean()

np.sqrt( np.sum(diff**2) / (len(ns_book7)-1) )
#**는 거듭제곱(제곱)

"""**최빈값 구하기**
기술통계의 마지막으로 최빈값에 대해 알아보겠습니다.
최빈값(mode) = 데이터에서 가장 많이 등장하는 값
앞서 출력한 descrive(include='object')의 출력 중에 top행에 나온 값이 바로 '최빈값'입니다.

판다스에서  mode()메서드 = 최빈값
도서명에서 가장 많이 등장하는 값을 찾아보면, describe()메서드와 출력이 동일한 것을 확인할 수 있습니다.
"""

ns_book7['도서명'].mode()

"""mode()메서드는 텍스트뿐만 아니라 수치형에도 적용할 수 있습니다.

남산도서관 대출 데이터에서 가장 많이 등장하는 연도는 무엇인지 알아봅시다.
"""

ns_book7['발행년도'].mode()

"""남산도서관은 2012년도 도서를 가장 많이 소장하고 있습니다.

지금까지 주로 판다스 시리즈 객체에서 메서드를 호출해 기술통계를 구하는 방법을알아보았습니다.

이런 메서드들은 대부분 판다스 데이터프레임에서도 직접호출할 수 있습니다.

**데이터프레임에서 기술통계 구하기**
지금까지 평균, 중앙값, min, max, 분위수, 분산, 표준편차를 구할 수 있는 다양한 판다스 메서드를 알아봤습니다.

이런 메서드들은 데이터프레임에도 사용할 수 있음.
수치열 형만 연산할 수 있기 때문에 해당 열에만 적용되도록 **numeric_onnly매개변수를 True**로 지정해야 합니다.
예로 들어, ns_book7데이터프레임에서 mean()메서드를 호출하면, 수치형인 네개의 열에 대한 평균값이 계산됩니다.

만약 numeric_only 매개변수를 지정하지 않으면, 모든 데이터 타입의 열에 대해 수행하기 때문에 시간이 매우 오래 걸리며, 경고가 발행합니다.
"""

ns_book7.mean(numeric_only=True)

"""위에처럼 mode()메서드도 데이터프레임에서 호출할 수 있습니다.
ns_book7의 '번호'열은 모두 고유값이라 최빈값이 의미없습니다.
따라서, loc메서드로 '도서명'열부터 마지막 열까지에 대해 최빈값을 찾으면 다음과 같습니다.
"""

ns_book7.loc[:, '도서명':].mode()

"""'도서명'열에서 가장 많이 등장한 값은 '승정원일기'이고, '출판사'열에서 가장 많이 등장하는 값은 '문학동네'입니다.

이때 오해하지 말아야 할 것.
**승정원일기가 문학동네 출판사의 도서라고 오해하지 말기**
mode()메서드 출력 결과물 사이에는 서로 연관이 없음을 주의하세요.

마지막으로, ns_book7데이터프레임을 csv파일로 저장합니다.
"""

ns_book7.to_csv('ns_book7.csv', index=False)

"""**넘파이의 기술통계 함수**
평균 구하기
"""

import numpy as np
np.mean(ns_book7['대출건수'])

##가중평균
np.average(ns_book7['대출건수'], weights=1/ns_book7['도서권수'])
#가중평균: 도서권수가 적은 책의 가치를 높게 평가
#1/도서권수를 가중치로 줬다는 건, 도서권수가 적을수록(분모가 작을수록) 가중치가 커진다
#"책이 딱 1권 있는데 10번 빌려 간 것"이 "책이 100권 있는데 10번 빌려 간 것"보다 더 가치가 높다고(인기가 많다고) 보고 싶을 때 사용

np.mean(ns_book7['대출건수']/ns_book7['도서권수'])
##비율의 평균: '권당 대출 효율'의 평균
##각 책마다 대출건수 / 도서권수를 계산해서 '한 권당 몇 번 빌려 갔나'라는 비율을 먼저 구한 뒤, 그 비율들의 평균을 낸 것

ns_book7['대출건수'].sum()/ns_book7['도서권수'].sum()
##전체 평균: 도서관 전체의 효율
##도서관에 있는 모든 대출건수를 다 더하고, 이를 모든 도서권수의 합으로 나눈 것

"""**중앙값 구하기**"""

np.median(ns_book7['대출건수'])

"""**최솟값, 최댓값 구하기**"""

np.min(ns_book7['대출건수'])

np.max(ns_book7['대출건수'])

"""**분위수 구하기**"""

## interpolation 매개변수가 numpy 1.22(python >= 3.8) 버전부터 method로 바뀜
np.quantile(ns_book7['대출건수'], [0.35,0.5,0.80])

"""**분산 구하기**"""

np.var(ns_book7['대출건수'])
#넘파이(NumPy)의 함수
#넘파이는 기본적으로 '모분산'을 계산합니다.
#즉, 전체 데이터의 개수(n)로 나누는 방식

ns_book7['대출건수'].var(ddof=0)
#판다스(Pandas)의 메서드
#판다스는 원래 통계 분석을 위해 만들어졌기 때문에, 기본적으로 '표본분산'을 계산합니다.
#즉, 전체 개수에서 하나를 뺀(n-1)로 나누는 방식
#하지만 뒤에 ddof=0을 붙여줌으로써, "판다스야, 너 원래 n-1로 나누지?
#근데 이번엔 넘파이처럼 그냥 $n$으로 나눠줘!"라고 강제로 설정을 바꾼 것

"""ddof는 Delta Degrees of Freedom(자유도)의 줄임말입니다. 쉽게 말해 "전체 개수($n$)에서 몇 개를 빼고 나눌 것인가?"를 결정하는 숫자입니다.

**표준 편차 구하기**
"""

np.std(ns_book7['대출건수'])

"""**최빈값 구하기**"""

values, counts = np.unique(ns_book7['도서명'], return_counts=True)
max_idx = np.argmax(counts)
values[max_idx]